version = "0.1.0-SNAPSHOT-03" //snapshots are supposed to go up every commit because i can't be bothered to delete the damn version every time i mess up ffs and if i run out of digits, idk what to do. maybe a major version bump? /shrug

dependencies {
	testImplementation("org.junit.jupiter:junit-jupiter:5.7.1")
	api("org.jetbrains:annotations:21.0.1")
	api("com.google.code.gson:gson:2.8.7")
}